#include <iostream>
#define MAX 1e9
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    int n,l;
    cin >> n >> l;
    int currentSize = l;
    while (l <= 100 && l <= n) {
        
    }
    cout << -1 << endl;
}